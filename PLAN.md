# 기획 — 직거래 임대 플랫폼 이용자 문의 응대 에이전트

작성: 2026-09-17. 개인 실습. 모두의연구소 Agt1 "[실습 프로젝트] 라우팅 에이전트 만들기"의 구조를 이 서비스에 적용해 봄 (제출물 아님).

## 1. 한 줄 정의

- 이용자(임차인·임대인) 문의 한 줄을 받아 카테고리 판정 → 해당 약관·방침 조항만 조회 → 조항 근거로만 답변 → 근거 밖 내용 검사
- 문서에 없거나 확신이 낮으면 운영자에게 넘김

## 2. 주제와 근거 문서

- 서비스: [korea-direct-rental](https://github.com/tigermorning/korea-direct-rental) (중개 없는 임대 직거래, 비공개 저장소)
- 근거 문서: 서비스에 실제로 게시하는 법적 고지 3종
  - 이용약관 13조 + 부칙 (13.6k자)
  - 개인정보처리방침 12항 (13.2k자)
  - 차별금지 가이드라인 8항 (7.8k자)
- 고른 이유
  - 조항 단위로 이미 나뉨 → 카테고리 매핑이 조항 번호로 떨어짐
  - 직거래라 이용자가 중개 서비스처럼 기대하는 질문이 많음 → 그럴듯한 오답이 자연스럽게 생김
  - 실습 결과를 서비스 운영(문의 응대 초안)에 그대로 쓸 수 있음
- 원본은 서비스 저장소의 `src/lib/legal/*.ts`. `scripts/export_docs`로 `docs/*.md`에 추출하고 추출 시점 커밋 해시를 기록

## 3. 카테고리 설계 (초안)

나눈 기준: 이용자 여정 단계 = 처리하는 담당 업무가 다름

| 카테고리 | 다루는 문의 | 근거 조항 |
|---|---|---|
| `ACCOUNT_PRIVACY` 계정·개인정보 | 가입·탈퇴, 수집 항목, 보유기간, 열람·삭제 요구 | 약관 4·5조 · 처리방침 1~3, 6, 7, 9항 |
| `LISTING` 매물 등록·소유 확인 | 등록 기준, 등기부 제출, 확인 배지, 임시 비공개 | 약관 6·12조 · 처리방침 2항(등기부) |
| `INTERVIEW` 방문·인터뷰 | 신청, 확정 후 연락처 공개, 재정정보 3항목, 거절 사유 | 약관 7조 · 가이드라인 2~5항 |
| `REPORT` 차별·허위매물 신고 | 차별 질문 받음, 허위매물, 신고 처리·제재 | 약관 8~10조 · 가이드라인 6·7항 |
| `OTHER` 범위 밖 | 중개수수료, 시세, 계약서 작성, 법률 자문, 보증금 사고 | 약관 3조(중개 아님) · 12조(책임 범위) — 안내만 |

- 넘기기(`ESCALATE`)는 카테고리가 아니라 별도 판단 — `classify`와 `gate` 노드 분리(어제 모두몰 구조 유지)
- 넘기기 조건 후보: 확신도 < 임계값, 문서에 근거 없음, 개별 계정 조회 필요("내 신고 처리됐나요")
- 확인할 것: 카테고리 간 겹치는 조항(약관 8조는 `INTERVIEW`·`REPORT` 둘 다) — 문서를 읽고 매핑 확정

## 4. 그럴듯한 오답 함정 (평가셋 `forbid` 후보)

| 문의 | 문서 안 읽으면 나오기 쉬운 답 | 문서 근거 |
|---|---|---|
| 중개수수료 얼마예요? | "0.4% 수준입니다" | 약관 3조 — 중개 아님, 수수료 없음 |
| 보증금 못 받으면 보상해 주나요? | "보증보험으로 보호됩니다" | 약관 12조 — 계약 당사자 아님 |
| 소유 확인 며칠 걸려요? | "1~2영업일" | 문서에 기간 없음 → 넘기기 |
| 임차인 직업·나이 물어봐도 되나요? | "참고용으로는 괜찮습니다" | 가이드라인 3항 — 수집하지 않는 정보 |
| 탈퇴하면 등기부 서류 바로 지워지나요? | "즉시 삭제됩니다" | 처리방침 3항 보유기간 |

## 5. 평가셋

- 규모: fewshot 15 + eval 40 이상 (강의 기준 최소 10건)
- 카테고리별 개수 균형 + 넘기기 정답 문항 20% 안팎
- 문항 필드: `query` · `route` · `expected_tools` · `must`(사실) · `forbid`(그럴듯한 오답) · `action`(ANSWER/ASK/ESCALATE) · `split`
- 작성자 분리 (어제 차별 감지기 교훈: 작성자 같은 셋은 만점, 블라인드는 0.64)
  - fewshot: 내가 작성, 프롬프트 예시로만 사용
  - eval: 프롬프트·규칙을 못 본 별도 에이전트가 문서만 보고 작성
  - `must`·`forbid`는 조항 번호를 함께 적어 근거 추적 가능하게
- 작성 후 사람 검토: 조항과 `must`가 실제로 맞는지

## 6. 파이프라인

```
query
  → classify (route + confidence)
  → gate (confidence·범위 판단, ESCALATE 분기)
  → assemble_context (route별 조항만)
  → answer (도구 호출 루프: get_terms / get_privacy / get_guideline / escalate)
  → verify (답변 문장·숫자가 조회한 조항에 있는지 대조)
  → 실패 시 재작성 1회 또는 넘기기
```

- LangGraph. 모두몰의 `router.py`·`context.py`·`guardrail.py`·`evaluate.py` 골격 재사용
- 도구 = 문서 조회 단위. 도구 호출 적절성은 기대 도구 집합과 정확히 일치할 때 1점
- verify: 모두몰은 숫자 출처 역추적만 했음. 여기선 숫자(기간·일수·금액) + 조항 번호 인용 대조 추가 검토

## 7. 측정

| 지표 | 방법 |
|---|---|
| 분류 | 정확도 · macro F1 · 혼동 행렬 (eval split) |
| 도구 호출 적절성 | 호출 집합 == 기대 집합 |
| 답변 적절성 | `must` 전부 포함 + `forbid` 위반 0. LLM 채점기에 "표현 말고 사실" 명시 |
| 채점기 검증 | 모범 답안을 넣어 전부 통과하는지 (`--validate`) |
| 넘기기 | 넘겨야 할 문항의 재현율 · 불필요한 넘기기 비율 |

- 실행 흔들림: 판정 바꾸는 실험은 2회 이상 측정
- 실험 기록표 `runs/LAB.md` — 한 번에 하나만 바꾸기

## 8. 데모

- Streamlit
- 화면: 입력창 · 답변 · 판정 카테고리/확신도 · 호출 도구 · 근거 조항 원문 펼치기 · 검증 결과(통과/걸린 문장)
- 공개 배포 선택 안 함. API 키는 `.env`만

## 9. 저장소

- 새 공개 저장소 `rental-support-agent` (서비스 저장소는 비공개 유지)
- 구조: `docs/` · `data/goldenset.json` · `prompts.py` · `context.py` · `tools.py` · `router.py` · `agent.py` · `guardrail.py` · `evaluate.py` · `app.py` · `runs/` · `REPORT.md`
- 모델: 모두몰과 같은 OpenAI 설정(`gpt-5.6-luna` 라우팅 / `gpt-5.6-terra` 답변) — 비교 가능하게

## 10. 작업 순서

1. 문서 추출 + 조항 통독 → 카테고리·매핑 확정
2. 평가셋 (fewshot 직접, eval 별도 에이전트) → 사람 검토
3. 모두몰 골격 이식 → 엔드투엔드 1회 실행
4. 채점기 + `--validate`
5. 기준선 측정 → 실패 직접 읽기 → 한 번에 하나씩 개선
6. Streamlit 데모 + 캡처
7. REPORT.md 7항목

## 11. 열린 결정

- 넘기기 임계값 초기값 (모두몰 0.5 그대로 시작?)
- `ASK`(되묻기) 행동을 둘지 — 계정 조회가 없는 에이전트라 대부분 `ESCALATE`로 흡수 가능
- 서비스 저장소 약관 원문 공개 범위 — 사업자 정보 플레이스홀더 외 민감 내용 없는지 추출 시 확인
